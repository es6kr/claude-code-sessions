# Claude Sessions Web Environment Variables
# Copy to .env for local development

# Editor command to open files (default: code)
# CLAUDE_SESSIONS_EDITOR=cursor

# Home directory for ~ expansion (default: system homedir)
# CLAUDE_SESSIONS_HOME=/Users/yourname

# Current project name for priority sorting (default: auto-detect from cwd)
# CLAUDE_SESSIONS_PROJECT=-Users-yourname-works-myproject
